# Open Escalations to User

This is what the PM needs from the User to keep moving. PM appends
new escalations at the bottom; resolves them by moving to the
`## Resolved` section with a short note. Open count is the top-line
indicator of how blocked the project is on a User decision.

---

## Open

### E1 — GitHub repo creation + initial push  ✅ RESOLVED

**Date:** 2026-04-26
**Resolved:** 2026-04-26
**Final state:** `main` pushed to
`https://github.com/treedoesdo2-hub/verbose-eureka.git` at SHA
`758b876`. Branch tracks `origin/main`. GCM auth went through after a
click on the "Sign in with your browser" dialog (PM had wrongly given
up on this before retrying — the dialog was up the whole time, behind
the terminal).

---

### E2 — master / main / mapgen-firefight-redesign reconciliation  ✅ RESOLVED (option A)

**Resolved:** 2026-04-26 — User picked option A. `mapgen-firefight-redesign`
renamed to `main`; `master` deleted locally; single trunk going forward.

(original entry below for history)



**Date:** 2026-04-26
**Asked of:** User
**Why:** Right now the named trunk is `master`. The actual trunk is
`mapgen-firefight-redesign` (49 commits ahead, 0 behind). `master`'s tip
is `cfabc07 Wire sim worker → main-process log pipeline` — unrelated
old work. Going forward we want a single canonical trunk named `main`
(per `pm/PROTOCOL.md` and library conventions).

**Three options PM sees:**

A. **Rename `mapgen-firefight-redesign` → `main`.** Keeps history.
   `master` becomes a dead branch we can delete or archive. Simplest.
   Loses the human-readable branch name "mapgen-firefight-redesign".
   PM recommendation.

B. **Merge `mapgen-firefight-redesign` into `master`, then rename `master` → `main`.**
   Preserves both names, but the merge commit is cosmetic (49 ahead,
   0 behind = fast-forward — there's nothing to merge).

C. **Leave it.** Keep working on `mapgen-firefight-redesign`. Tools that
   assume `main` (like `/ultrareview`) will keep failing. Not recommended.

**What I need from you:** A/B/C decision.

---

### E3 — Fabricated PR numbers in historical commits  ✅ RESOLVED (option C)

**Resolved:** 2026-04-26 — User picked option C. 117 commits rewritten
via `git filter-branch --msg-filter`; `(#NNN)` and `(#NNN / #NNN)` refs
stripped, `(increment 2)` / `(increment 3)` markers preserved. Done
before first push so no force-push needed.

(original entry below for history)



**Date:** 2026-04-26
**Asked of:** User
**Why:** Multiple historical commits reference PR numbers like `(#280)`,
`(#288 / #478)`, `(#291 / #514)`, `(#533)`. None of these correspond to
real PRs — there's no remote and never has been. They were generated by
prior agents to mimic a PR workflow.

Going forward, this is a hard NO per `pm/CHARTER.md`. The question is
**what do we do about historical commits.**

**Three options:**

A. **Leave them.** History is messy but immutable; future commits are
   clean. Cost: someone reading `git log` is misled. PM recommendation
   if we want zero risk of force-push.

B. **Add a one-time `pm/HISTORICAL_NOTES.md`** that explains the
   fabrication and lists affected commits. Same history, but a
   permanent footnote. Easy + honest. PM second-choice recommendation.

C. **Rewrite history (interactive rebase / `git filter-repo`).**
   Strips the fake refs from messages. Requires force-push. Should
   only be done **before** the first push to GitHub (i.e., now, while
   no one else has the history). Most invasive, but cleanest result.

**What I need from you:** A/B/C decision. If C, do it now (before E1's
push); if A or B, no urgency.

---

## Resolved

(none yet — file just initialized 2026-04-26)
